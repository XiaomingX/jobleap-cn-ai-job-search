1
请简要描述你对Transformer架构的理解，以及它在大模型中的核心作用。
回答注意事项：
能否准确阐述Transformer的核心组件（如自注意力机制、多头注意力、编码器/解码器结构）及关键技术细节（如位置编码、残差连接）。
能否清晰说明Transformer相比传统循环神经网络（RNN/LSTM）的优势（如并行计算能力、长序列依赖建模能力），并解释其在大模型（如GPT、LLaMA）中的架构支撑作用。

2
在大模型训练过程中，你认为数据预处理（如数据清洗、格式转换）和超参数调优（如学习率、batch size）分别对模型性能有哪些关键影响？请举例说明。
回答注意事项：
能否分析数据预处理的核心环节（如数据去重、噪声过滤、数据增强）及对模型收敛速度、泛化能力的影响（如未清洗数据导致梯度震荡）。
能否说明超参数对训练稳定性的作用（如学习率过高导致梯度爆炸/震荡，batch size过小影响梯度估计准确性），并结合具体场景（如小数据集微调vs大规模预训练）分析参数选择逻辑。

3
你是否使用过Hugging Face Transformers或类似大模型框架？请举例说明你用它完成过哪些具体任务（如模型加载、微调、推理部署），遇到过哪些问题及如何解决？
回答注意事项：
能否展示对工具的实际操作能力（如模型加载（`from_pretrained`）、微调（`peft`库）、推理加速（`device_map`）等关键API的使用熟练度）。
能否描述问题排查的逻辑（如加载失败时的日志分析、显存不足时的模型量化/分块策略）及解决过程的合理性（如通过`torch.save`断点续训、使用`accelerate`库实现分布式推理）。

4
在大模型分布式训练中（如数据并行、模型并行），你了解哪些常见技术方案？请简述数据并行和模型并行的核心区别及适用场景。
回答注意事项：
能否准确说明分布式训练的主流策略（如Megatron-LM的模型并行+数据并行组合、DeepSpeed的ZeRO优化），并区分数据并行（模型复制、梯度聚合）和模型并行（拆分层/张量到多设备）的核心逻辑。
能否结合模型规模（如10亿级参数vs100亿级参数）分析适用场景（如单卡训练小模型用数据并行，多卡训练超大规模模型用模型并行+混合并行）。

5
如果让你在一周内上手基于LLaMA或GPT类模型进行简单的指令微调（如对话模型微调），你会通过哪些步骤和资源快速掌握相关技术？
回答注意事项：
能否规划清晰的学习路径：如第一步查阅官方文档（Hugging Face/模型原作者GitHub），第二步设计微调数据集（格式标准化、prompt构造），第三步使用LoRA/QLoRA等高效微调方法，第四步验证与调优（验证集指标、混淆样本分析）。
能否列举可靠学习资源（如技术博客、开源项目代码、社区论坛讨论），并说明如何利用资源解决关键问题（如LoRA参数高效微调的原理与实现）。

6
请手写一个简单的Python函数，实现对输入文本的分词处理（仅用基础Python实现，避免引入复杂库），并说明你在代码中如何考虑可读性和边界情况。
回答注意事项：
能否写出逻辑清晰的分词函数（如按空格分割、特殊字符过滤、大小写统一），并通过注释或变量命名体现代码可读性（如`filter_punctuation`函数处理标点，`normalize_text`函数统一格式）。
能否考虑边界情况（如输入为空字符串、全角/半角字符、数字/符号混合文本）并在代码中通过条件判断（如`if not text: return []`）或异常处理（如`try-except`捕获UnicodeDecodeError）保障健壮性。

7
假设你需要设计一个简单的大模型推理服务（支持单模型或多模型切换，如对话模型+问答模型），你会考虑哪些核心组件和技术细节？
回答注意事项：
能否系统性列举组件：如模型加载模块（模型加载优先级、动态加载策略）、请求处理模块（异步队列、请求优先级）、推理引擎（TensorRT/ONNX Runtime加速）、服务部署（Docker容器、Kubernetes调度、负载均衡）。
能否分析关键技术细节（如模型量化（INT8/INT4）降低显存占用、流式推理（Streaming Decoding）提升大模型响应速度、模型版本管理（A/B测试））。

8
当你在使用大模型框架进行模型训练时，遇到训练过程中loss不收敛（持续震荡或上升）或模型过拟合（训练集准确率高但验证集低），你会如何排查和解决？请列举至少三种可能原因及对应方案。
回答注意事项：
能否从多维度排查问题：数据层面（数据分布偏移、标签错误）、模型层面（结构冗余、过深/过宽）、训练参数层面（学习率/正则化系数不合理）、硬件层面（显存不足导致精度下降）。
能否对应解决方案的合理性：如数据层面用数据增强/正交验证集，模型层面简化结构/加入Dropout，参数层面动态调整学习率（如余弦退火），硬件层面使用混合精度训练（FP16/BF16）。

9
大模型在实际应用中面临计算资源和推理速度的挑战，你了解哪些模型压缩或加速技术？请选择一种技术（如量化、剪枝、知识蒸馏），说明其原理、实现步骤及适用场景。
回答注意事项：
能否准确描述技术原理：如量化（通过降低参数精度（如FP32→FP16）减少模型大小和计算量）、剪枝（移除低权重连接降低参数量）、知识蒸馏（用大模型“教”小模型，保留关键特征）。
能否结合场景说明适用性：如移动端部署用INT8量化（牺牲精度换速度），大规模预训练模型用剪枝（保留核心参数），边缘设备用知识蒸馏（训练轻量级模型替代大模型）。

10
在你过往的学习或项目中，是否有过需要快速学习新技术并与团队协作完成任务的经历？请简述过程、你的角色及最终成果。
回答注意事项：
能否清晰描述学习过程（如通过开源社区文档、导师指导、技术博客快速掌握核心知识点），并体现主动性（如主动查阅相关论文、复现基础实验）。
能否说明团队协作中的角色（如需求拆解、技术选型、代码评审、结果验证），并量化成果（如任务提前2天完成、模型性能提升X%）。

